# Changesets

This directory contains pending changesets for `@zooid/zoon-web`.

Run `pnpm changeset` from the repo root to create a new changeset.
Run `pnpm changeset version` to apply pending changesets and bump versions.
Run `pnpm changeset publish` to publish to npm (after `pnpm build`).
